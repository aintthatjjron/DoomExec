#include <windows.h>
#include <psapi.h>
#include <iostream>

void hideFromYara() {
    std::cout << "[*] Hiding memory from YARA..." << std::endl;
    // now we just have to actually figure this bs out
}
